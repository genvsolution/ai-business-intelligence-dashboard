# -----------------------------------------------------------------------------
# General Flask Application Settings
# -----------------------------------------------------------------------------

# The entry point for your Flask application.
# Example: 'app.py' or 'wsgi.py' if using a factory pattern.
FLASK_APP=app.py

# The environment the application is running in.
# 'development' for local development, 'production' for live servers, 'testing' for tests.
# This affects Flask's debug mode, error handling, and other behaviors.
FLASK_ENV=development

# Set to 'True' for development to enable debug mode (auto-reloader, debugger).
# ALWAYS set to 'False' in production for security and performance.
DEBUG=True

# A strong, random secret key used by Flask for signing session cookies,
# CSRF tokens, and other security-related functions.
# GENERATE A LONG, RANDOM STRING FOR PRODUCTION (e.g., using os.urandom(24).hex()).
SECRET_KEY=your_flask_app_secret_key_that_is_very_long_and_random_example

# Application name, used for display purposes (e.g., in emails, page titles).
APP_NAME="Sales Analytics Dashboard"


# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# Main database URL for production (PostgreSQL).
# Format: postgresql://user:password@host:port/dbname
# Example: postgresql://sales_user:secure_password@localhost:5432/sales_db
DATABASE_URL=postgresql://user:password@host:5432/dbname_example

# SQLite database path for local development and testing.
# This will be used if FLASK_ENV is 'development' or 'testing'
# and a PostgreSQL DATABASE_URL is not explicitly configured or prioritized.
# Example: instance/dev.db (relative to project root)
SQLITE_DATABASE_PATH=instance/dev.db

# Optional: SQLAlchemy track modifications setting.
# Set to 'False' to save memory if you don't need Flask-SQLAlchemy's event system.
SQLALCHEMY_TRACK_MODIFICATIONS=False


# -----------------------------------------------------------------------------
# Security & Session Management
# -----------------------------------------------------------------------------

# A separate strong, random secret key for Flask-WTF's CSRF protection.
# Can be the same as SECRET_KEY, but often recommended to be different for layered security.
# GENERATE A LONG, RANDOM STRING FOR PRODUCTION.
WTF_CSRF_SECRET_KEY=your_csrf_secret_key_example

# Enable or disable CSRF protection globally for Flask-WTF forms.
# ALWAYS set to 'True' in production.
WTF_CSRF_ENABLED=True


# -----------------------------------------------------------------------------
# Email Configuration (for password resets, notifications, etc.)
# -----------------------------------------------------------------------------

# SMTP server hostname.
MAIL_SERVER=smtp.mailtrap.io

# SMTP server port. Common ports: 25, 465 (SSL), 587 (TLS).
MAIL_PORT=2525

# Enable/disable Transport Layer Security (TLS). Set to True for most modern SMTP servers.
MAIL_USE_TLS=True

# Enable/disable Secure Sockets Layer (SSL).
# Usually, either TLS or SSL is used, not both. Set to True if your server uses SSL on port 465.
MAIL_USE_SSL=False

# Username for SMTP authentication.
MAIL_USERNAME=your_email_username_example

# Password for SMTP authentication.
MAIL_PASSWORD=your_email_password_example

# Default sender email address for system emails (e.g., password resets).
MAIL_DEFAULT_SENDER="no-reply@yourdomain.com"


# -----------------------------------------------------------------------------
# AI/ML Integrations (for commercial LLM APIs)
# -----------------------------------------------------------------------------

# The primary LLM provider to use (e.g., 'openai', 'anthropic', 'local').
# This allows switching between different LLM services based on configuration.
LLM_PROVIDER=openai

# API Key for OpenAI services (if LLM_PROVIDER is 'openai').
OPENAI_API_KEY=sk-your_openai_api_key_example

# API Key for Anthropic services (if LLM_PROVIDER is 'anthropic').
ANTHROPIC_API_KEY=sk-ant-api03-your_anthropic_api_key_example

# The specific LLM model to use from the chosen provider.
# Example for OpenAI: 'gpt-4-turbo', 'gpt-3.5-turbo'
# Example for Anthropic: 'claude-3-opus-20240229', 'claude-3-sonnet-20240229'
LLM_MODEL=gpt-4-turbo


# -----------------------------------------------------------------------------
# Caching & Asynchronous Task Queues (Redis, Celery)
# -----------------------------------------------------------------------------

# URL for the Redis server, used for caching frequently accessed data.
# Format: redis://[:password]@host:port/db_number
# Example: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# Celery broker URL. This is where Celery tasks are sent and queued.
# Can be Redis, RabbitMQ, etc.
# Example (Redis, using a different DB than cache): redis://localhost:6379/1
CELERY_BROKER_URL=redis://localhost:6379/1

# Celery result backend URL. This is where Celery stores task results/states.
# Can be Redis, database, etc.
# Example (Redis, using a different DB): redis://localhost:6379/2
CELERY_RESULT_BACKEND=redis://localhost:6379/2


# -----------------------------------------------------------------------------
# Monitoring & Logging
# -----------------------------------------------------------------------------

# Sentry Data Source Name (DSN) for error tracking and performance monitoring.
# Example: https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_DSN=your_sentry_dsn_example


# -----------------------------------------------------------------------------
# Initial Setup / Admin Defaults (for development/first run, NOT for production)
# -----------------------------------------------------------------------------

# Default username for the initial admin user created on first run (if applicable).
# This should be changed immediately after setup in production.
DEFAULT_ADMIN_USERNAME=admin

# Default password for the initial admin user.
# This should be a strong, temporary password and changed immediately.
DEFAULT_ADMIN_PASSWORD=change_this_password_immediately_example

# Default email for the initial admin user.
DEFAULT_ADMIN_EMAIL=admin@yourdomain.com


# -----------------------------------------------------------------------------
# Other Application-Specific Settings
# -----------------------------------------------------------------------------

# Base URL for the application, useful for generating absolute URLs in emails or APIs.
# Example: https://app.yourdomain.com
APP_BASE_URL=http://localhost:5000

# Number of items to display per page in paginated lists (e.g., lead listings, user tables).
ITEMS_PER_PAGE=20