# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
*/env/
*/venv/
*/.venv/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.nox/
.eggs/
*.egg
*.egg-info/
dist/
build/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.env
.flaskenv
instance/
celerybeat-schedule

# IDEs and Editors
.idea/                          # PyCharm
.vscode/                        # VS Code
*.sublime-project               # Sublime Text
*.sublime-workspace             # Sublime Text
*.swp                           # Vim swap files
*.swo                           # Vim swap files
*~                              # Emacs backup files
.#*                             # Emacs lock files

# Operating System Files
.DS_Store                       # macOS
Thumbs.db                       # Windows
ehthumbs.db                     # Windows
.directory                      # Linux
.Trash-*                        # Linux

# Database
*.db                            # SQLite database files
*.sqlite
*.sqlite3
/data/*.db                      # If databases are stored in a data directory
/data/*.sqlite
/data/*.sqlite3

# Logs and Caches
logs/
*.log
*.tmp
.cache/
tmp/

# Frontend (Node.js/NPM if used for Bootstrap, Chart.js, jQuery)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.parcel-cache/
.next/
out/ # Next.js build output

# AI/ML Specific
# Trained models (can be large, often stored in cloud storage or separate model registry)
*.pkl
*.joblib
*.h5
*.keras
*.onnx
*.pb # TensorFlow saved models
model_weights/
# Large datasets or intermediate data files
data/raw/                       # Raw data, often too large or sensitive for Git
data/processed/                 # Processed data, if generated and large
# NLTK/SpaCy data downloads (if not managed within venv)
nltk_data/
spacy_data/

# Docker
docker-compose.override.yml     # Local overrides, often contains sensitive info
.dockerignore                   # This file itself should be versioned, but good to remember its purpose

# Gunicorn/uWSGI specific (if they create temporary files)
*.sock                          # Unix sockets
*.pid                           # Process IDs

# Flask-specific
# If you have specific upload folders that shouldn't be tracked
uploads/

# Security related (sensitive files that should NEVER be committed)
# Although .env covers most, sometimes other files might be created.
*.key
*.pem
*.crt
*.p12

# Test and coverage artifacts
.coverage.*
.coveragerc
coverage.xml
.pytest_cache/

# Documentation generation
_build/
api_docs/ # If generated locally and not meant for VCS

# Miscellaneous
*.zip
*.tar.gz
*.rar
*.7z
*.bak
*.orig
*.patch
*.diff